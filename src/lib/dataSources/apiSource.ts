export async function processFromApi(runId: string, userId: string) {
    // Future implementation: Fetch from API and map to ExcelEngine compatible format
    console.log("Fetching from API for run:", runId);
    return { data: [], errors: [] };
}
